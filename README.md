# wb-order-engine-replenishment

## Запуск локально
```bash
pip install -r requirements.txt
uvicorn server.main:app --reload
```
Открой в браузере: http://127.0.0.1:8000
Перетащи отчёты → «Собрать валидационный инпут» → «Скачать Excel».
